import QtQuick 2.15
import QtQuick.Controls.Styles 1.4

/*CircularGaugeStyle {

        needle: Rectangle {
            y: outerRadius * 0.15
            implicitWidth: outerRadius * 0.03
            implicitHeight: outerRadius * 0.9
            antialiasing: true
            color: Qt.rgba(0.66, 0.3, 0, 1)
        }

}*/
Rectangle {
    id: newPage
    anchors.fill: parent
    color: "lightblue"

    // Add the content for your new page here
}
